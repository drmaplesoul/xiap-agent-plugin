# App Config 工作流

## 执行步骤

### 1. 配置分析
- 了解当前应用配置结构和使用的配置源（环境变量 / 配置文件 / 配置中心）
- 确认需要变更的配置项及其影响范围

### 2. 变更方案
- 生成配置变更清单
- 评估变更风险和回滚方案
- 区分环境差异（dev / staging / prod）

### 3. 配置更新
- 更新配置文件或配置中心
- 记录变更版本号
- 验证新配置语法和取值

### 4. 生效确认
- 调用 `mcp__myplugin__deploy_check` 确认环境状态
- 必要时触发应用重载配置
- 输出变更记录
