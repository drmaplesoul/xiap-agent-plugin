# App Deploy 工作流

## 执行步骤

### 1. 部署前检查
- 调用 `mcp__myplugin__deploy_check` 确认目标环境状态
- 检查依赖服务可用性
- 确认版本号和变更清单

### 2. 执行部署
- 按环境顺序执行（dev → staging → prod）
- 每个环境部署后执行冒烟测试
- 记录部署日志

### 3. 部署验证
- 调用 `mcp__myplugin__db_query` 验证数据一致性
- 检查应用健康检查端点
- 确认关键功能可用

### 4. 回滚预案
- 保留上一版本制品
- 准备回滚命令
- 监控异常告警
